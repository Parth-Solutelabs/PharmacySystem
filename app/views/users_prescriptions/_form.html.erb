<%= form_with(model: users_prescription) do |form| %>
  <% if users_prescription.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(users_prescription.errors.count, "error") %> prohibited this users_prescription from being saved:</h2>

      <ul>
        <% users_prescription.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
